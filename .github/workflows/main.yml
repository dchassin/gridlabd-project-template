name: Simulation

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    run-on: ubuntu-latest
    container: slacgismo/gridlabd:latest
    
    steps:
    - uses: actions/checkout@v2

    - name: Run simulation
      run: gridlabd input/*.glm | tee output/console.txt
      
    - name: Save results
      uses: actions/upload-artifact@v2
      with:
        name: output
        path: output/
      
